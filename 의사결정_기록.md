# 소규모학교 통폐합 시뮬레이터 — 의사결정 기록

이 문서는 개발 과정에서 내려진 세부 의사결정을 시간순으로 남겨, 추후 **연구보고서**와 **요구사항정의서 개정판** 작성 시 근거자료로 쓰기 위한 기록이다. 새로운 결정이 있을 때마다 이 문서 하단에 항목을 추가한다.

각 항목은 아래 형식을 따른다: **날짜 / 항목 / 결정 내용 / 근거 / 관련 문서·절**

---

## 2026-08-11

### 1. 요구사항정의서 11절 미결 사항 6개 전항목 확정 → v1.0

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| 1 | 배포/권한 구조 | 단일 시스템(공유계정 kedi) + 향후 RBAC 확장 가능 구조. 교육지원청별 개별 배포(멀티테넌시)는 미채택. 실제 계정 분리는 3차로 이연 | 176개 이상 인스턴스 개별 운영·갱신 부담이 커 멀티테넌시 비권장(요구사항정의서 4.1) |
| 2 | 시범지역(파일럿) | **경기도 연천교육지원청** | 보유한 국토정보플랫폼 500m 격자 인구 데이터가 경기도 31개 시군구 전체를 커버(zip 파일 직접 확인). 2025년 xlsx 학생수 데이터를 초등학교 60명 이하 기준으로 직접 집계한 결과, 연천교육지원청은 13개교 중 9개교(69%)가 소규모학교로 경기도 내 최고 비율. 학교 수가 적어(13개교) MVP 검증이 빠르고, 1개 군=1개 교육지원청이라 GIS·통계 집계가 단순 |
| 3 | 시군구 GIS 경계 출처 | 통계청 **SGIS(통계지리정보서비스) Open API** | 웹 검색으로 확인: 로그인 후 무료 인증키 발급 가능, 표준 행정경계 SHP/GeoJSON 제공(sgis.kostat.go.kr) |
| 4 | 학교 좌표(위경도) 출처 | 공공데이터포털 **"전국초중등학교위치표준데이터"**(한국교육시설안전원, 매월 갱신) | 웹 검색으로 확인: 위도·경도 컬럼을 직접 포함하고 있어 나이스 API 기반 지오코딩이 불필요함 |
| 5 | 수용 상한(C̄_j) 초기 기본값 | 지역·학교급별 별도값 없이 **1,080명 단일 기본값 유지**, 사용자가 조정 가능한 파라미터로 제공 | 선행연구(CRR 2026) 참고값과 동일. 지역별 실측 근거 없이 세분화하는 것은 과도한 정밀화로 판단 |
| 6 | 이동시간 보정계수 | **1차 MVP는 미적용**(카카오 API 자동차 기준 시간 그대로 사용), 2차 이후 재검토 | 스쿨버스 실제 소요시간 대비 보정계수를 뒷받침할 실측 자료가 없어 임의 계수를 도입하지 않음. 알려진 모형 한계로 문서에 명시(7.7절) |

반영 파일: [소규모학교_통폐합_시뮬레이터_요구사항정의서_v1.0.docx](소규모학교_통폐합_시뮬레이터_요구사항정의서_v1.0.docx) (원본 v0.1은 보존)

### 2. 화면설계 와이어프레임 v0.1 작성

- 범위: 로그인 + 요구사항정의서 5절의 6단계 화면(기초자치단체 선정 → 학교 옵션 선택 → 기본통계 → 학생거주정보 → 시뮬레이션 실행 → 결과 생성) 전체
- 형태: 저해상도 와이어프레임(배치·정보구조·데이터 연동만 확정, 색상·아이콘 등 비주얼 디자인은 미정) — HTML 아티팩트로 게시
- 각 화면에 데이터 소스·연동 API·확정 사항을 주석 태그로 병기하여 요구사항정의서 절 번호와 상호 참조되도록 구성
- **상태: 초안. 추후 변경 가능** — 개발 진행하면서 화면 배치나 항목이 바뀌면 이 기록과 요구사항정의서 관련 절을 함께 갱신할 것

---

## 2026-08-11 (계속)

### 3. 프로젝트 스캐폴드 및 DB 스키마 v0.1

- 폴더 구성: `backend/`(FastAPI+ETL), `frontend/`, `data/{raw,processed}/`, `db/`
- [db/schema.sql](db/schema.sql): PostgreSQL+PostGIS 기준 스키마 1차안 — region/education_office/school/school_year_stat/population_grid/travel_time_cache/simulation_run·result/app_user
- **DB 실행 환경 미정**: 로컬 개발 환경에 PostgreSQL 미설치 상태. ETL은 `--dry-run`으로 로직만 검증했고, 실제 적재는 DB 인스턴스 확보 후 진행 필요 (사용자 확인 필요 — Docker 로컬 설치 vs 클라우드 관리형 중 택1)

### 4. CSV↔xlsx 컬럼 매핑 및 연천 파일럿 데이터로 ETL 검증

- [db/컬럼매핑_CSV_xlsx.md](db/컬럼매핑_CSV_xlsx.md) 작성 — CSV는 학교코드가 없어 xlsx 기준 학교명 텍스트 매칭으로 연결해야 함을 확인
- [backend/etl/load_school_stats.py](backend/etl/load_school_stats.py)를 연천교육지원청 실데이터로 실행 검증(`--dry-run`):
  - 학교 19개(초13+중6) 정상 식별, xlsx 38행 + CSV 209행(2014~2024) 정상 매칭 → 학교당 2014~2026년 13개년 연속 시계열 확보(예: 백학초등학교 46명(2014)→61명(2018 정점)→33명(2026))
  - **매칭 실패 9건 확인**: "연천초등학교고문분교장"(2016 휴교, 2017 폐교), "전곡초등학교적동분교장"(2018 폐교) — 현재 xlsx 명단에 없는 과거 분교. 이미 소규모학교 통폐합이 일어난 실제 이력이므로 삭제하지 않고 `school_year_stat_unmatched.csv`에 별도 보존(추후 "과거 통폐합 사례" 참고자료로 연구보고서에 활용 가능)
  - 결론: 연천 규모(19개교)에서는 학교명 매칭 정확도 100%(현존 학교 기준). 전국 확장 시 동명이교 등 재검증 필요(컬럼매핑 문서에 명시)

## 2026-08-11 (계속) — 기술스택 전략 전환 (중요)

### 5. 1차 MVP 구현 스택을 React+FastAPI+PostgreSQL → **Streamlit 단일 앱**으로 변경

- **결정**: 최종보고회(간트차트 기준 8/29) 시연용 1차 MVP는 PostgreSQL/PostGIS DB 적재를 보류하고, **Streamlit** 기반 경량 파이썬 앱으로 구현한다. 지도·통계·시뮬레이션(CPMP)까지 하나의 Streamlit 앱 안에서 세션 상태로 처리하고, 데이터는 DB 대신 정제된 파일(data/processed/*.csv, 추후 확보되는 GIS·좌표 파일)을 직접 읽어 쓴다.
- **근거**: 사용자 결정(2026-08-11) — "DB는 나중에 하도록 하고, 스트림릿으로 하고, 최종보고회까지 간다." 로컬에 PostgreSQL 환경이 없는 상태에서 19일 일정 내 React+FastAPI+DB 3단 구조를 갖추는 것보다, 데이터·알고리즘 검증 및 발표용 시연에는 Streamlit이 훨씬 빠르다.
- **향후 계획**: 이 프로젝트가 (연구보고회 등에서) **승인되면, 프로덕션 단계에서는 Lovable 클라우드와 연계**하여 정식 서비스로 전환할 예정. 즉 현재 스택은 "검증·시연용 임시 스택"이며, 요구사항정의서 9절의 React+FastAPI+PostgreSQL+PostGIS 제안은 **프로덕션 전환 시점의 목표 스택으로 유지**한다(폐기 아님).
- **영향**:
  - `db/schema.sql`, `backend/etl/load_school_stats.py` 등 기존 산출물은 프로덕션 전환 시 재사용 목적으로 보존(당장 실행 대상 아님)
  - 지도 표시는 카카오맵 JS SDK를 Streamlit에 HTML 컴포넌트로 임베드하는 방식을 우선 검토(요구사항정의서 5.1/5.2절의 카카오맵 요건 유지)
  - CPMP 솔버는 DB 없이 Streamlit 세션 내 메모리에서 직접 실행(PuLP/OR-Tools), 결과도 세션 상태로 유지(새로고침 시 소실 — 데모 특성상 허용)
  - **관련 요구사항정의서 절**: 9절(기술스택 제안)은 차기 개정판에서 "1차 MVP(임시)"와 "프로덕션 목표" 두 단으로 구분 명시 필요(아직 미반영, TODO)

### 6. Streamlit MVP 1차 구현 및 실행 검증

- [app/streamlit_app.py](app/streamlit_app.py): 로그인+6단계 전체 화면 구현, session_state로 위저드 방식 진행
- [app/data_access.py](app/data_access.py): DB 없이 data/processed/*.csv 직접 로드
- [backend/algo/cpmp.py](backend/algo/cpmp.py): CPMP MILP 솔버(PuLP/CBC), 요구사항정의서 7.2절 수식 그대로 구현 + 7.5절 교원수 보조식. 합성 데이터로 정상해/불능해(infeasible) 양쪽 케이스 단위 검증 완료
- [backend/etl/load_grid_population.py](backend/etl/load_grid_population.py): 보유한 국토정보플랫폼 zip에서 연천군 500m 격자 shapefile(EPSG:5179)을 추출해 위경도(EPSG:4326)로 변환. **검증 결과**: 초등 상주인구 합계 1,210명(35개 격자), 중등 601명(21개 격자) — 같은 지역 재학생수 합계(약 1,351명, school_dim 기준)와 근사한 규모로 정합성 확인
- 브라우저로 실제 앱 구동 검증(로그인 → 1~3단계): 백학초등학교 선택 시 2014~2026년 실데이터 추이 차트, 소규모학교 판정(🔴), 교육지원청 내 백분위(하위 15%) 정상 출력 확인
- **버그 수정 2건**: (1) pandas Series를 `or` 연산자로 참 거짓 판정하려다 발생한 ValueError, (2) 이전 선택값 복원 로직의 불필요한 인덱싱 오류 — 둘 다 브라우저 실행 중 발견 즉시 수정

### 7. 남은 데이터 공백 (5단계·6단계 시뮬레이션 실행 불가 상태)

- **학교 좌표(위경도)가 아직 없어 CPMP 시뮬레이션(5·6단계)을 실행할 수 없음.** 확정 소스(전국초중등학교위치표준데이터, 5.2절)를 실제로 내려받는 작업이 남음 — 공공데이터포털 파일 다운로드는 사용자 확인 후 진행 예정
- 시군구 GIS 경계(SGIS)도 마찬가지로 소스만 확정, 실제 연동 전(1단계는 중심점 마커로 임시 대체 중)
- 이동시간(d_ij)은 카카오모빌리티 API 키 미발급 상태라 **직선거리 기반 근사치**로 임시 대체(UI에 명시적으로 경고 표시)

### 8. 학교 좌표 실데이터 확보 및 5·6단계(CPMP 시뮬레이션) 최초 정상 작동

- 사용자 승인(2026-08-11) 후 공공데이터포털 "전국초중등학교위치표준데이터"(publicDataPk=15021148, 한국교육시설안전원)를 실제로 확보. 데이터셋 페이지의 다운로드 API(`/download/standard.json`, 인증 불필요)를 통해 전국 12,011행 중 연천교육지원청 소속 21행(고등학교 2개교 제외 시 19개교)을 직접 조회
- [backend/etl/load_school_coordinates.py](backend/etl/load_school_coordinates.py): 원본 스냅샷(data/raw/school_locations_yeoncheon_raw.json) → school_dim.csv와 학교명 기준 매칭 → data/raw/school_locations.csv. **19/19개교 전원 매칭 성공**(연천 규모에서는 학교명 매칭이 완전히 안정적임을 재확인)
- 좌표 확보로 5·6단계(CPMP 시뮬레이션 실행·결과)가 최초로 실제 작동. 브라우저로 로그인→1→2(19개교 전체 선택)→3→4→5→6단계까지 전체 흐름을 실제로 구동해 확인:
  - p=10(13개교 중 10개교 존치), 수용상한 1,080명, 이동시간제한 30분 조건에서 정상해 도출
  - 총 배정학생수 1,210명(격자 인구 전량 배정), 목적함수(총 가중이동시간) 2,645(분·명)
- **버그 수정 1건**: `load_school_coordinates()`가 반환하는 df에 `school_name` 컬럼이 남아있어 다른 테이블과 병합 시 `school_name_x`/`school_name_y`로 충돌 → `KeyError`. 반환 컬럼을 `[school_code, lat, lon]`으로만 제한해 해결
- **프로세스 노트**: Streamlit은 진입 스크립트(streamlit_app.py)는 저장 시 자동 재실행하지만, `import data_access as da` 같은 하위 모듈은 프로세스가 완전히 재시작되기 전까지 이전 버전이 `sys.modules`에 캐시된 채로 남아 있어 수정이 반영되지 않을 수 있음(실제로 이 때문에 위 버그가 한 번 더 재현되어 프로세스를 강제 종료 후 재기동해서야 해결됨) — 앞으로 data_access.py 등 하위 모듈을 수정하면 서버를 완전히 재시작할 것

이 시점 기준(2026-08-11) **1차 MVP 6단계 전체가 실데이터로 로그인부터 결과 생성까지 end-to-end 정상 작동함을 확인**. 남은 공백은 시군구 GIS 경계 폴리곤(SGIS 미연동, 1단계는 중심점 마커로 대체 중)과 실제 도로망 이동시간(카카오모빌리티 API 키 미발급, 현재 직선거리 근사 사용) 두 가지.

### 9. 2026-08-11 작업 마감 요약

오늘 하루 진행된 작업을 정리하면:
1. 요구사항정의서 11절 미결 사항 6개 전항목 확정 → v1.0
2. 로그인+6단계 화면설계 와이어프레임 v0.1 작성
3. 기술스택을 React+FastAPI+PostgreSQL(1차)에서 **Streamlit 단일 앱(임시, 최종보고회용)**으로 전환 — DB/PostGIS 스키마와 백엔드 골격(db/schema.sql, backend/etl/load_school_stats.py)은 프로덕션 전환 시 재사용할 목적으로 보존만 함
4. CPMP 솔버(backend/algo/cpmp.py), 격자 인구 ETL, 학교 통계 ETL 구현·실데이터 검증
5. 공공데이터포털에서 학교 좌표 실데이터 확보(19/19개교 매칭)
6. Streamlit 앱 로그인~6단계 전체를 실데이터로 브라우저 구동 검증 완료(정상 작동)

**다음 세션에서 이어갈 후보 작업(우선순위 미정, 사용자 확인 필요):**
- 시군구 GIS 경계(SGIS) 연동 — 현재 1단계 지도는 중심점 마커만 표시
- 카카오모빌리티 API 키 발급 및 실제 도로망 이동시간(d_ij) 연동 — 현재 직선거리 근사치 사용
- UI 다듬기(최종보고회 발표용)
- 요구사항정의서 9절(기술스택)에 "1차 MVP(Streamlit, 임시)"와 "프로덕션 목표(React+FastAPI+PostgreSQL, 승인 후 Lovable 클라우드 연계)" 2단 구분을 반영해 개정 (현재 미반영 상태)

**프로젝트 파일 현황**: 프로젝트 폴더 최상위에 원본 데이터·산출 문서, `app/`(Streamlit), `backend/`(ETL+알고리즘, 프로덕션용 스캐폴드 포함), `data/{raw,processed}/`(정제 데이터), `db/`(프로덕션 스키마 설계) 구조로 정리되어 있음. 실행: `streamlit run app/streamlit_app.py`.

## 2026-08-11 (계속) — 카카오모빌리티 실제 이동시간 연동

### 10. 카카오모빌리티 API 키 확보 및 실제 도로망 이동시간(d_ij) 연동

- 사용자가 KAKAO_REST_API_KEY를 채팅으로 직접 제공(2026-08-11). **주의**: 대화 로그에 원본 키가 남으므로, 카카오 개발자센터에서 키 재발급(rotate)을 권고함. 코드에는 하드코딩하지 않고 프로젝트 루트 `.env`(KAKAO_REST_API_KEY=...)에만 저장, `.gitignore`에 `.env` 등록
- 실제 길찾기(`https://apis-navi.kakaomobility.com/v1/directions`) 호출로 키 권한 확인(200 정상 응답) — 별도 카카오모빌리티 B2B 계약 없이도 일반 REST API 키로 Directions API 사용 가능함을 확인
- [backend/etl/kakao_travel_time.py](backend/etl/kakao_travel_time.py): API 클라이언트(`get_api_key`, `kakao_minutes`) 구현
- [backend/etl/load_travel_time_matrix.py](backend/etl/load_travel_time_matrix.py): 연천군 500m 격자(상주인구>0, 초35+중21=56개) x 동일 학교급 학교(초13+중6=19개) 전 쌍 = **581쌍 전량 조회 성공**(실패 0건). 결과: `data/processed/travel_time_matrix_연천군.csv`(평균 16.3분, 최소 0.3분, 최대 46.5분). 재실행 시 이미 조회된 쌍은 건너뛰어 API 호출을 아끼도록 구현
- [backend/algo/cpmp.py](backend/algo/cpmp.py): `solve_cpmp`에 `distance_matrix` 파라미터 추가(하위호환 — 기본값 None이면 기존 haversine 근사 `distance_fn` 그대로 사용). 매트릭스에 있는 쌍은 실측치, 없는 쌍만 근사치로 자동 대체
- [app/data_access.py](app/data_access.py)에 `load_travel_time_matrix()` 추가, [app/streamlit_app.py](app/streamlit_app.py) 5단계가 매트릭스 존재 여부에 따라 안내 캡션(✅ 실제 도로망 vs ⚠ 근사치)을 동적으로 표시하도록 수정
- 브라우저로 로그인→1~6단계 전체 재검증: 동일 조건(p=10, 수용상한 1,080명, 이동시간제한 30분)에서 목적함수(총 가중이동시간)가 근사치 기준 2,645(분·명) → 실측치 기준 **4,624(분·명)**로 변화 확인. 직선거리 근사가 실제 도로망 소요시간을 상당히 과소평가하고 있었음을 확인 — 연천군은 산간지형이 많아 도로가 직선이 아니기 때문으로 추정(원인 분석은 연구보고서 작성 시 참고자료로 활용 가능)
- 남은 것: 실행 스크립트의 마지막 안내 print문에서 이모지 아닌 특수문자(em dash)가 Windows 콘솔(cp949)에서 UnicodeEncodeError를 일으키는 사소한 버그를 발견해 수정함(CSV 저장 자체는 그 이전에 끝나 데이터 손실 없음)

이 시점 기준 남은 공백은 **시군구 GIS 경계(SGIS) 미연동** 하나로 좁혀짐(1단계 지도가 여전히 중심점 마커).

## 2026-08-12 — SGIS 행정구역경계 연동 (요구사항정의서 5.1절 확정 소스 전면 반영 완료)

### 11. SGIS Open API 인증키 확보 및 연천군 경계 폴리곤 연동

- 사용자가 sgis.kostat.go.kr에서 직접 회원가입/로그인해 테스트 키(consumer_key, consumer_secret)를 발급받아 채팅으로 제공(2026-08-12). 계정 생성·로그인은 정책상 대신 해줄 수 없는 작업이라 사용자가 직접 진행 — 카카오 때와 달리 이 단계는 위임 불가능함을 확인
- `.env`에 SGIS_CONSUMER_KEY/SGIS_CONSUMER_SECRET 저장(코드 하드코딩 없음)
- **SGIS 자체 시도코드가 통계청 표준 행정구역코드와 다름을 실측으로 확인**: 경기도가 표준코드로는 41이지만 SGIS API에서는 `31`(서울11·부산21·대구22·인천23·광주24...로 이어지는 자체 체계). 문서화되지 않은 부분이라 API를 직접 호출해 시도 목록을 받아본 뒤 확인함 — 이후 경기도 산하 시군구(low_search=1) 44개 중 "연천군" 문자열로 필터링(adm_cd=31550으로 직접 지정하지 않고 이름 매칭 방식을 택함 — 학교명 매칭 때와 같은 이유로 코드값보다 이름 매칭이 이 프로젝트에서 더 안정적이라고 판단)
- **좌표계 이슈 발견 및 수정**: SGIS 응답 좌표가 UTM-K(EPSG:5179)임을 확인(격자인구 ETL과 동일 이슈). pyproj로 EPSG:5179 → EPSG:4326(WGS84) 재투영 후 저장하도록 [load_admin_boundary.py](backend/etl/load_admin_boundary.py) 수정 — 최초 실행 시 이 변환이 빠져 좌표가 (95만대, 196만대) 같은 미터 단위로 저장되는 버그가 있었고, 재투영 후 (경도 127.09, 위도 38.28)로 정상 확인
- [backend/etl/sgis_boundary.py](backend/etl/sgis_boundary.py): accessToken 발급 + hadmarea.geojson 조회 클라이언트
- [app/data_access.py](app/data_access.py)에 `load_admin_boundary()` 추가, [app/streamlit_app.py](app/streamlit_app.py) 1단계 지도가 경계 파일 존재 여부에 따라 실제 폴리곤(folium.GeoJson) vs 중심점 마커를 동적으로 전환하도록 수정
- 브라우저로 재검증: 1단계에 "✅ 시군구 경계는 통계청 SGIS Open API 실측 폴리곤을 사용합니다" 캡션 표시, folium iframe 내부에 leaflet 폴리곤 경로(path.leaflet-interactive) 1개 렌더링 확인(좌표가 연천군 실제 위치와 일치)

**이 시점 기준(2026-08-12) 요구사항정의서 6절의 데이터 소스 4가지(학생수 통계, 학교좌표, 격자인구, 이동시간)에 5.1절 GIS 경계까지 더해 전부 확정 소스로 연동 완료.** 남은 것은 UI 다듬기·통합테스트 문서화·9절 기술스택 2단 구분 반영 등 비-데이터 항목뿐.

## 2026-08-12 (계속) — 통합테스트 시나리오 및 카카오 키 재발급

### 12. 통합테스트 시나리오 문서화

- [통합테스트_시나리오.md](통합테스트_시나리오.md) 작성 — 로그인·1~4단계 데이터 표시·5~6단계 CPMP 13개 시나리오를 브라우저 실구동으로 검증
- **발견 1(UI 개선 필요)**: 이동시간 제한을 매우 짧게 주면(예: 3분) 코드가 "불능해"로 판단하지 않고 도달 불가능한 격자를 조용히 배정에서 제외한다(`cpmp.py`의 의도된 동작). 그런데 6단계 결과 화면이 "총 배정학생수"만 보여줄 뿐 전체 학생 수 대비 몇 명이 빠졌는지 알려주지 않아, 사용자가 이 사실을 놓치기 쉽다. UI 다듬기 단계에서 "총 배정학생수(전체 N명 중)" 형태로 병기하거나 경고 배지를 추가하도록 권고
- **발견 2(재확인)**: 카카오모빌리티 실측 이동시간이 haversine 근사치보다 74% 크게 나옴(목적함수 4,624 vs 2,645, 동일 조건). 연구보고서에 "근사치 기반 분석은 이동 부담을 과소평가한다"는 근거로 활용 가능
- 테스트 중 Streamlit range 슬라이더가 자동화 도구의 단순 DOM value 설정으로는 반영되지 않고 클릭+키보드 조작이 필요함을 확인 — 실사용자 조작에는 영향 없는 테스트 자동화 한정 이슈

### 13. 카카오·SGIS 키 재발급 (둘 다 완료)

- 사용자가 Kakao Developers에서 새 앱("소규모학교통폐합선정시뮬레이터")을 만들어 카카오 REST API 키를 재발급, `.env` 갱신 완료. 새 키로 길찾기 API 재검증(22.0분, 기존 값과 근접 — 실시간 교통량 차이로 추정) — 정상 작동 확인
- 사용자가 sgis.kostat.go.kr에서 SGIS 서비스ID/보안Key도 재발급, `.env` 갱신 완료. 새 키로 accessToken 발급 재검증 — 정상 작동 확인
- 채팅에 노출됐던 기존 카카오·SGIS 키 4개는 전부 재발급으로 무효화됨

### 14. 6단계 결과화면 UI 수정 — 미배정 학생 수 표시

- 발견 1(항목 12)에 따라 `app/streamlit_app.py`의 5단계에서 `ss.sim_params`에 `total_population`(선택된 학교급의 전체 상주인구)을 저장하고, 6단계에서 `총 배정학생수` 옆에 `(전체 N명 중)`을 병기 + 미배정 인원이 0.5명을 넘으면 경고 배지를 추가하도록 수정
- 브라우저로 재검증: 이동시간제한 3분 케이스에서 "이동시간 제한(3분) 내에 어떤 학교에도 배정되지 못한 학생이 959명 있습니다" 경고 정상 표시, 기본 케이스(30분)에서는 경고 없이 "총 배정학생수=1,210명 (전체 1,210명 중)"만 표시되는 것도 함께 확인(회귀 없음, 목적함수 4,624 재현)
- [통합테스트_시나리오.md](통합테스트_시나리오.md) T12 항목 및 발견사항 갱신

### 15. 요구사항정의서 9절 — 1차 MVP(임시) vs 프로덕션 목표 2단 구분 반영

- `python-docx`로 v1.0.docx의 9절을 직접 편집(사본 백업 후 편집, XML well-formed·zip 무결성 확인 후 백업 삭제)
- 기존 "9. 기술 스택 제안" 표(React+FastAPI+PostgreSQL 등)의 헤더를 "제안" → "프로덕션 목표(승인 후 전환 시 적용)"로 명확화
- 새 하위섹션 "9.1 1차 MVP(2026-08-11~08-29) 실제 구현 스택 — 임시" + 새 표 추가: 프론트엔드(Streamlit+folium/SGIS, 당초 검토한 카카오맵 JS SDK 대신), 백엔드(API 서버 없음), 솔버(PuLP, 변경 없음), DB(미사용), 외부 API(SGIS·카카오모빌리티 연동 완료, 카카오맵 SDK는 미사용), 배포(로컬 실행)
- 전환 배경(의사결정_기록 2026-08-11 항목 5 인용)과 2026-08-12 기준 데이터 5종 연동 완료 상태를 설명 문단으로 병기
- 8/11 작업 마감 요약에 남겼던 TODO("9절에 2단 구분 반영 필요")를 해소함

**이 시점 기준(2026-08-12) 사용자가 지시한 "1순위→4순위 순차 진행"이 전부 완료됨**: ①카카오·SGIS 키 재발급 ②통합테스트 시나리오 문서화 ③6단계 미배정 학생 수 UI 수정 ④요구사항정의서 9절 개정.

### 16. CPMP 정책 변경 — 미배정 학생 발생 시 경고 대신 불능해로 처리

- 항목 14에서 "6단계에 경고 배지 표시"로 완화했던 것을, 사용자가 "빠진 학생이 있으면 처음부터 '목적함수를 찾을 수 없습니다' 불능해로 처리하고 5단계에서 다시 시작하게 하라"고 명시적으로 요청해 정책을 더 엄격하게 재변경
- **배경**: 미배정 학생은 실제로 "학교를 못 다니는" 게 아니라 "이 조합(p·수용상한·이동시간제한)으로는 어느 학교도 시간 내에 도달 못 하는 비현실적 파라미터 조합"이라는 신호다. 경고만 띄우고 부분해를 그대로 보여주면, 연구자가 목적함수 값이 전체 학생 기준이 아니라는 걸 놓친 채 보고서에 인용할 위험이 여전히 남는다고 판단 — 아예 계산을 막고 재조정을 강제하는 편이 더 안전하다는 결론
- [backend/algo/cpmp.py](backend/algo/cpmp.py): `solve_cpmp`가 LP를 구성하기 전에 먼저 모든 수요지에 이동시간 제한 내 후보 학교가 있는지 검사. 하나라도 없으면 즉시 `status="infeasible"`, `message="목적함수를 찾을 수 없습니다: ..."`로 반환(격자 개수·학생 수 명시). 기존 "후보 없는 격자는 제약 생성 생략" 로직은 이제 도달 불가능(unreachable) 사전 체크로 대체되어 항상 참인 조건이 됐으므로 제거
- [app/streamlit_app.py](app/streamlit_app.py) 6단계의 미배정 경고 배지(항목 14에서 추가)는 이제 도달할 일이 없는 코드라 제거하고, `(전체 N명 중)` 표시만 유지(항상 전량 배정임을 보여주는 용도로 남김) — 5단계는 기존에 이미 infeasible 시 같은 화면에 머물러 재시도 가능하도록 되어 있어 별도 수정 불필요
- 브라우저로 재검증: 시간제한 3분 → "목적함수를 찾을 수 없습니다: ... 수요지가 22개(학생 959명) 있습니다" 표시, 5단계에 그대로 머묾. 시간제한 30분(정상 케이스)은 회귀 없이 그대로 재현(목적함수 4,624, 1,210명 전량 배정)
- [통합테스트_시나리오.md](통합테스트_시나리오.md) T12 및 발견사항 갱신

### 17. 요구사항정의서 7.3·7.6절 — 새 불능해 정책 반영

- 항목 16에서 `cpmp.py`에 반영한 정책 변경(이동시간 제한 내 도달 불가 수요지가 있으면 즉시 불능해)이 문서에는 빠져 있던 것을 보완
- 7.3절 "이동시간 제한" 항목에 2026-08-12 확정 문구 추가: 도달 불가 수요지가 있으면 부분해 대신 전체를 불능해로 처리한다는 내용, 7.6절 참조 지시
- 7.6절 "예외 처리"에 불능해 원인을 (1) 수용 상한 초과, (2) 이동시간 제한 내 도달 불가 수요지 존재 — 두 가지로 명시하고, 둘 다 "목적함수를 찾을 수 없습니다" 문구로 안내하며 5단계에서 즉시 재시도 가능함을 기술
- python-docx로 기존 단락에 문장을 추가하는 방식으로 편집(사본 백업 후 XML well-formed·zip 무결성 확인 후 백업 삭제) — 9절 편집 때와 동일한 절차

### 18. 새 불능해 정책 스트레스 테스트 — 연천군 규모의 실제 임계값 확인

- 항목 16의 정책 변경(이동시간 제한 내 도달 불가 수요지 존재 시 즉시 불능해)이 실사용 범위에서 과도하게 자주 막히지는 않는지 브라우저로 3개 시나리오 추가 검증([통합테스트_시나리오.md](통합테스트_시나리오.md) T14~T16)
- **중학교 p=1(6개교 중 1개교만 존치), 수용상한 1,080명, 시간제한 30분(기본값)**: 정상해(목적함수 3,097, 601명 전량 배정) — 가장 극단적인 통폐합 시나리오도 기본 시간제한에서는 문제없음을 확인
- **초등 p=10, 시간제한 15분(기본값의 절반)**: 정상해, 목적함수가 30분 조건(4,624)과 완전히 동일 — 즉 원래 최적해가 애초에 전부 15분 이내였다는 뜻으로, 연천군 규모에서는 15분까지 안전
- **초등 p=10, 시간제한 10분**: 불능해 — 딱 1개 격자(학생 7명)만 도달 불가로 걸림. 15분과 10분 사이 어딘가가 이 지역·조건에서 정책이 실제로 작동하기 시작하는 경계선임을 확인
- **결론**: 새 정책은 현실적인 정책 논의 범위(15분 이상)에서는 과잉 반응하지 않고, 비현실적으로 빡빡한 조건(10분 이하)에서만 정확히 걸린다. 다만 이 임계값(10~15분)은 연천군 특유의 지리적 조건에 따른 것이라 전국 확장 시 지역별로 다시 확인해야 함 — 통합테스트_시나리오.md에 명시해 둠

### 19. 통합테스트 미완료 항목 확인 + 발표용 UI 다듬기

- **설립구분 필터**: 연천군 19개교가 전부 "공립" 단일값임을 `school_dim.csv`로 확인 — 테스트할 대상 자체가 없어 종료 처리. 세션 충돌·전국 확장 성능은 1차 MVP 범위 밖으로 유지(사유는 기존과 동일)
- **UI 다듬기**: 구체적인 발견 사항 없이 임의로 디자인을 손대는 대신, 지도 마커 색상에 대응하는 범례 캡션과 6단계 존치/폐교 학교 수 요약 지표를 추가하는 선에서 진행 — 브랜딩·색상 등 더 깊은 디자인 결정은 사용자 확인 없이 임의로 정하지 않음
  - 2단계 지도 아래에 "🔴 소규모학교 · 🔵 일반학교" 범례 추가(기존엔 마커 호버 툴팁에만 있었음)
  - 6단계에 "존치(거점학교) N개교 / 폐교대상 M개교" 지표(st.metric) 추가, 지도 아래 "🟢 존치(거점학교) · ⚪ 폐교대상" 범례 추가
  - 브라우저로 재검증: p=10 기본 조건에서 "존치 10개교 / 폐교대상 3개교" 정상 표시, 목적함수 4,624 회귀 없음

**이 시점 기준(2026-08-12) "순차적으로 수행하라"고 지시된 4개 후속 작업 전부 완료**: ①7.6절 갱신 ②불능해 정책 스트레스 테스트(임계값 10~15분 확인) ③통합테스트 미완료 항목 정리 ④발표용 UI 다듬기.

### 20. 요구사항정의서 5.6절 미구현 항목 발견 및 구현 — 학구경계 시각화·통학시간 분포

- 5.6절(6단계 결과 생성 화면 구성요소)을 실제 구현과 대조해 두 가지 항목이 1차 MVP 범위인데도 아직 구현되지 않았음을 발견: (1) "재배치된 학구 경계(X_ij 결과) 시각화", (2) "학교별... 통학시간 분포(평균/최대)". (참고: "시나리오 비교"·"결과 내보내기(PDF/Excel)"는 10절 로드맵에 "2차"로 명시돼 있어 범위 밖이 맞음 — 혼동하지 않도록 로드맵을 먼저 확인하고 보고함)
- [backend/algo/cpmp.py](backend/algo/cpmp.py) `CpmpResult`에 `travel_minutes`(grid_id → 배정된 학교까지의 실제 d_ij) 필드 추가, `solve_cpmp`가 배정 결과와 함께 반환하도록 수정
- [app/streamlit_app.py](app/streamlit_app.py) 6단계에 구현:
  - 격자별 배정 학교를 학교마다 다른 색(`CATCHMENT_PALETTE`, 13색)으로 지도에 폴리곤 표시, 존치 학교 마커도 같은 색으로 통일, 폐교대상은 회색 유지, HTML 범례(학교명별 색상 스와치) 추가
  - 결과표에 "평균 통학시간(분)"·"최대 통학시간(분)" 컬럼 추가(학생수 가중평균·최댓값, `travel_minutes`와 격자 인구 기준)
- **검증 방법**: `st.dataframe`이 canvas 기반(glide-data-grid)이라 브라우저 텍스트 추출로 표 값을 직접 읽을 수 없음 — 동일 로직(`data_access` + `cpmp.solve_cpmp`)을 별도 파이썬 스크립트로 재현해 목적함수(4624.25)·총 배정(1,210명)이 UI 표시값과 일치함을 확인. 지도·범례는 브라우저에서 정상 렌더링 확인
- **부수 발견**: 존치 학교 10개 중 1개(연천노곡초등학교)는 배정 학생 0명 — 원 연구 수식 그대로인 제약3(Σ Y_j = p, 등식)의 성질상 나올 수 있는 정상 현상(버그 아님). 평균/최대 통학시간 컬럼은 이 경우 빈 값(None)으로 정상 처리됨을 확인. 발표 시 이런 학교가 나오면 별도 설명이 필요할 수 있음 — [통합테스트_시나리오.md](통합테스트_시나리오.md) T17에 기록

**이 시점 기준(2026-08-12) "순차적으로 수행하라"고 지시된 4개 후속 작업 전부 완료**: ①7.6절 갱신 ②불능해 정책 스트레스 테스트(임계값 10~15분 확인) ③통합테스트 미완료 항목 정리 ④발표용 UI 다듬기. 이어서 요구사항정의서 5.6절 대조 중 발견한 1차 MVP 미구현 항목 2개(학구경계 시각화, 통학시간 분포)도 구현·검증 완료.

### 21. 요구사항정의서 5.3절 미구현 항목("학급당 학생수") 구현 + 사용자 요청으로 "교사1인당 학생수" 추가

- 5.6절에 이어 5.3절도 대조해 "최신연도 학급당 학생수, 학급수, 교사수, 특수학급 현황 표" 중 **학급당 학생수**만 구현이 빠져 있었음을 발견(학급수·학생수·교사수·특수학급은 개별 지표로 이미 표시 중이었으나 나누기 파생값은 계산되지 않았음)
- 사용자가 "학급당 학생수와 함께 교사1인당 학생수도 함께 보여달라"고 요청 — 원 문서 5.3절에는 없던 지표라 확장으로 처리
- [app/streamlit_app.py](app/streamlit_app.py) 3단계: 기존 4개 지표(학급수·학생수·교사수·특수학급)를 왼쪽 서브컬럼에, 신규 파생 지표(학급당 학생수, 교사1인당 학생수)를 오른쪽 서브컬럼에 배치해 "원자료 그대로 vs 계산값"을 시각적으로 구분
- 브라우저로 검증: 백학초등학교(학급수 7, 학생수 33, 교사수 7) 기준 학급당 학생수 4.7명, 교사1인당 학생수 4.7명(우연히 학급수=교사수라 같은 값) 정상 표시
- 요구사항정의서 5.3절에 "교사1인당 학생수는 원안에 없던 항목, 2026-08-12 사용자 요청으로 확장"이라고 명시(python-docx로 편집, XML·zip 무결성 확인 후 백업 삭제)

**이 시점 기준(2026-08-12) 요구사항정의서 5절(단계별 기능요구사항) 전체를 실구현과 대조 완료** — 발견된 미구현 항목(5.3절 학급당 학생수, 5.6절 학구경계 시각화·통학시간 분포)은 전부 구현·검증·문서 반영까지 마쳤다. 시나리오 비교·PDF내보내기(2차 로드맵)를 제외하면 1차 MVP 범위의 문서-구현 간극은 남아있지 않음.

### 22. 학생1인당 기본적/선택적 교육활동비 지표 추가 — 2025년 학교 예결산 자료 신규 연동

- 사용자가 2025년 학교회계/사립학교 교비회계 예·결산서 세출자료 4개(초등 예산세출, 중등 결산세출 × 공립/사립)를 제공하며 "학생1인당 기본적교육활동비·학생1인당 선택적교육활동비도 병기, 2025년 한 해 자료로, 기존 학생수자료와 연동"을 요청
- 원본 파일을 `data/raw/budget_2025/`에 저장(사용자 Downloads 폴더에서 프로젝트로 복사, 원본은 그대로 둠)
- [backend/etl/load_school_budget.py](backend/etl/load_school_budget.py): 4개 xlsx를 각각 읽어 `정보공시 학교코드`가 연천 19개교 코드와 일치하는 행만 필터링해 병합. **19/19개교 전량 매칭 성공**(전부 공립 파일에서만 매칭 — 사립 파일 2종은 연천에 해당 학교가 없어 0건, 예상대로) → `data/processed/school_budget_2025.csv`(school_code, basic_edu_activity_krw, elective_edu_activity_krw, seoutguse_gubun, fiscal_year)
- **데이터 특이사항**: 사용자가 제공한 자료가 초등은 예산세출(계획), 중등은 결산세출(실적)로 서로 다른 종류였음 — 임의로 통일하지 않고 `seoutguse_gubun` 컬럼에 그대로 남겨 UI 캡션에 노출(투명성 유지, 6.3절 데이터 검증 원칙과 동일한 태도)
- [app/data_access.py](app/data_access.py): `load_school_budget()`, `year_stat(code, year)` 추가(특정 연도 학생수 조회용 — 기존 `latest_year_stat()`은 최신연도만 반환해 2025년 고정 조회에는 부적합)
- [app/streamlit_app.py](app/streamlit_app.py) 3단계에 두 지표 추가, **2025년 학생수**(2026년 아님)로 나눠 계산 — 캡션에 "2025년 예결산 세출(OO세출) ÷ 2025년 학생수 N명 기준"을 명시해 어느 연도 학생수를 썼는지 항상 보이게 함
- 브라우저로 검증: 백학초등학교 기준 학생1인당 기본적교육활동비 1,414,359원(55,160,000÷39), 선택적교육활동비 1,866,128원(72,779,000÷39) — 2025년 학생수 39명(2026년 33명과 다름) 기준으로 정확히 계산됨을 확인
- 요구사항정의서 5.3절에 이 확장(원안에 없던 항목, 출처, 학교급별 예산/결산 혼재 사실)을 명시(python-docx 편집, XML·zip 무결성 확인)
- [통합테스트_시나리오.md](통합테스트_시나리오.md) T19 추가

### 23. 경기도 전역 확대 착수 — 데이터 계층(1단계: 학교통계·행정경계·격자인구)

- 사용자 제안(연천군으로 검증 끝났으니 경기도 전체 학생거주정보가 이미 있으니 확대하면 어떤가)에 대해, "경기도 전체를 미리 다 계산" 대신 "데이터 커버리지는 경기도 전체로 넓히되, 비용이 드는 카카오모빌리티 이동시간 계산은 사용자가 실제 선택한 지역만 그때그때"하는 방향을 제안했고 사용자가 승인
- **학교통계(school_dim/school_year_stat)**: `load_school_stats.py --office-contains 경기도`로 재생성. 이 과정에서 실제 버그 발견·수정: CSV(2014-2024)의 "시도" 컬럼이 "경기도"가 아니라 "경기"(접미사 없는 축약형)라 시도 단위 필터가 0건 매칭되던 것을 `CSV_SIDO_ALIAS` 매핑으로 수정 → 수정 후 학교 2,050개교, CSV 매칭 20,968행(매칭실패 325개교, 학교명 변경·폐교 이력 추정, 기존에 알려진 한계와 동일 종류). 연천 19개교 코드는 전량 그대로 보존됨을 확인(회귀 없음)
- **행정경계**: [load_admin_boundary.py](backend/etl/load_admin_boundary.py)를 연천군 필터링 없이 SGIS 응답 전체(44개 시군구/일반구)를 저장하도록 변경 → `admin_boundary_경기도.geojson`. 어차피 SGIS가 경기도 전체를 한 번의 호출로 반환하므로 추가 비용 없음
- **격자인구**: [load_grid_population.py](backend/etl/load_grid_population.py)에 `--all` 옵션 추가, zip 안의 31개 시군구를 순회해 `population_grid_경기도.csv`(90,344행) 하나로 통합 저장. 연천 값(초등 1,210명/35격자, 중등 601명/21격자)이 정확히 그대로 재현됨을 확인(회귀 없음)
- **지역 단위 불일치 발견**: 세 데이터 소스의 지역 granularity가 다름 — school_dim은 대도시 7곳(수원·성남·안양·부천·안산·고양·용인·화성)을 일반구 단위로 47개 지역으로 쪼개 제공하는 반면, SGIS 경계와 격자인구는 시 단위로만 제공(예: "고양시" 하나, "고양시 덕양구" 등은 없음). 화성시는 school_dim에만 4개 구(동탄·만세·병점·효행)로 더 세분화되어 있고 SGIS엔 아예 "화성시" 하나뿐 — 앱에서 구 단위 지역 선택 시 상위 시 단위 경계/격자로 자동 대체하는 매칭 로직이 필요함(다음 단계에서 구현 예정)
- **차단된 부분**: 학교 좌표(위경도)는 현재 연천 19개교만 있음(`school_locations_yeoncheon_raw.json`이 연천만 필터링된 스냅샷). 원본 공공데이터포털 "전국초중등학교위치표준데이터"는 파일 다운로드에 CAPTCHA가 걸려 있어 자동화 불가 — 대신 같은 데이터셋의 Open API(`https://api.data.go.kr/openapi/tn_pubr_public_elesch_mskul_lc_api`, 개발단계 자동승인)를 쓰기로 하고, 사용자에게 활용신청·서비스키 발급을 요청함(카카오·SGIS와 동일 패턴 — 로그인이 필요한 절차라 대신 할 수 없음)

## 2026-08-12 (계속) — 경기도 전역 확대 완료 (학교 위치 API 연동·지역 파라미터화·온디맨드 이동시간 계산)

### 24. data.go.kr Open API로 학교 좌표 확보 + 지역 선택 인터랙티브화 + 이동시간 온디맨드 계산 구현·검증

- 항목 23에서 차단됐던 학교 좌표(위경도) 문제를 해결: 사용자가 data.go.kr에서 "전국초중등학교위치표준데이터" 같은 데이터셋의 Open API 활용신청을 완료해 서비스키를 발급(2026-08-12). 최초 접근 시 일반 문의게시판으로 잘못 진입한 것을 정확한 경로(데이터셋 상세페이지 → 오픈 API 탭 → 활용신청 버튼)로 재안내해 해결
  - 서비스키가 퍼센트 인코딩된 형태로 표시되어 `urllib.parse.unquote()`로 디코딩 후 `.env`(DATA_GO_KR_SERVICE_KEY)에 저장
  - [backend/etl/data_go_kr_client.py](backend/etl/data_go_kr_client.py) 신규 작성(카카오·SGIS와 동일한 `.env` 로딩 패턴), [backend/etl/load_school_locations_gyeonggi.py](backend/etl/load_school_locations_gyeonggi.py) 신규 작성 — `tn_pubr_public_elesch_mskul_lc_api` 엔드포인트를 페이지네이션으로 전량 조회(경기도교육청 소속 2,558행, 3페이지)
  - [backend/etl/load_school_coordinates.py](backend/etl/load_school_coordinates.py): 원본을 연천 전용 스냅샷에서 경기도 전역 스냅샷으로 교체. **버그 발견·수정**: 병합 키가 `[학교명, 학교급]`뿐이라 "동명이교"(예: 탑동초등학교가 동두천양주·수원 두 교육지원청에 각각 존재) 13개교(26행)가 중복 매칭됨 — `office_name`(교육지원청)을 세 번째 병합 키로 추가해 해결, 이후 2,037개교 전량 고유 매칭·중복 0건 확인. 연천 19개교는 좌표값 완전히 동일하게 재현(회귀 없음)
- **data_access.py 지역 파라미터화**: `PILOT_OFFICE` 상수 제거, 모든 로더 함수(`schools_in_region`, `load_population_grid`, `load_admin_boundary`, `load_travel_time_matrix`)가 region 인자를 받아 경기도 통합 파일에서 그때그때 필터링하는 방식으로 재작성. `load_admin_boundary`는 대도시 7곳이 SGIS에서 여러 일반구 폴리곤으로 나뉘어 오는 것을 고려해 단일 dict 대신 리스트를 반환하도록 변경
  - **버그 발견·수정(office_percentile 범위 오류)**: `school_dim`이 연천 19개교(0.9%)에서 경기도 2,050개교로 확대되면서, 기존 `office_percentile()`이 학교급만으로 비교 대상을 잡던 로직이 "동일 교육지원청 내 백분위"라는 원래 의도와 달리 조용히 경기도 전체 기준으로 계산하게 되는 것을 코드 리뷰 중 발견 — `office_name` 일치 조건을 추가해 수정. 회귀 테스트: 연천 백학초등학교 백분위 15.38%로 수정 전후 동일함을 확인
- **1단계 지역선택 인터랙티브화**: [app/streamlit_app.py](app/streamlit_app.py) 1단계에 `st.selectbox`로 경기도 31개 시군구 선택 UI 추가, 지역 변경 시 선택 학교·시뮬레이션 결과를 초기화. 2~6단계 전체가 `ss.selected_region`을 참조하도록 수정(5단계는 기존에 `da.load_school_dim()`으로 전국 학교를 CPMP 후보로 넘기던 버그도 함께 발견해 `da.schools_in_region()`으로 수정)
- **이동시간 온디맨드 계산**: 카카오모빌리티 실측 이동시간은 지역마다 미리 계산해 두기엔 비용이 크므로(경기도 전역 선계산 대신 항목 23에서 승인된 "선택한 지역만 그때그때" 전략), 5단계에서 해당 지역의 매트릭스 파일이 없으면 쌍 수·예상 소요시간을 안내하고 사용자가 버튼을 눌러야 계산이 시작되도록 구현. [backend/etl/kakao_travel_time.py](backend/etl/kakao_travel_time.py)에 `compute_matrix()`(progress_cb 콜백 지원) 추가, `st.progress()`로 진행률 표시
- **브라우저 전체 검증(과천시 신규 지역 + 연천군 회귀)**:
  - 1단계: 과천시 선택 시 "경기도 과천시 → 경기도안양과천교육지원청"으로 정상 전환(연천과 다른 교육지원청명이 정확히 다르게 resolve됨), SGIS 경계 폴리곤 정상 렌더링
  - 2단계: 과천시 9개교(과천초·관문초·문원초·청계초·과천갈현초·과천율목초·과천문원중·과천중·과천율목중) 정상 표시, 전부 "일반학교"(소규모 아님 — 신도시라 타당한 결과)
  • 3단계: office_percentile 버그 수정이 실제로 작동함을 확인 — "경기도안양과천교육지원청 내 규모 백분위"로 정확한 교육지원청명 표시, 백분위 값이 학교마다 다양하게(32~100%) 나와 안양+과천 통합 동료집단과 비교되고 있음을 확인. 예산 지표는 연천 전용 파일(`school_budget_2025.csv`)이라 과천에서는 정상적으로 생략됨(graceful degradation)
  - 4단계: "과천시 500m 격자 중 초등학교 상주인구 > 0 인 격자 33개, 합계 5,818명" 정상 표시
  - 5단계: 온디맨드 계산 UI가 "격자×학교 288쌍, 약 2.4분 소요" 안내를 정확히 표시. "지금 계산하기" 버튼 클릭 → `st.progress()`가 1→288까지 실시간 갱신되는 것을 확인(총 약 4.5분 소요, 예상보다 API 응답이 느려 실제로는 초당 약 1쌍 처리) → 완료 후 자동 재실행되어 "✅ 실제 도로망 이동시간 사용(288쌍 확보)"로 전환 확인. `data/processed/travel_time_matrix_과천시.csv` 289행(헤더+288) 생성 확인 — 캐시-후-재사용 설계가 실제로 작동함
  - 6단계: 기본 파라미터(p=3, 수용상한 1,080명, 30분)는 과천시 규모(6개 초등학교에 5,818명)에는 너무 타이트해 불능해 발생 — 이는 버그가 아니라 항목 16에서 확정한 "도달 불가 시 즉시 불능해" 정책이 새 지역에서도 올바르게 작동한 것. p=6(최대)·수용상한 2,000명·시간제한 60분으로 조정하자 정상해 도출(목적함수 24,680, 문원초·청계초·과천갈현초 3개교 존치, 5,818명 전량 배정) — 지도·존치/폐교 지표 정상 렌더링
  - **회귀 확인**: 1단계에서 연천군으로 재전환 후 2~5단계를 다시 진행 — 4단계 "연천군 500m 격자... 35개, 1,210명"(기존과 동일), 5단계에서 기존에 저장돼 있던 `travel_time_matrix_연천군.csv`(581쌍)를 온디맨드 계산 없이 즉시 "✅ 실제 도로망 이동시간 사용" 상태로 불러옴을 확인 — 지역별 캐시 파일 분리가 의도대로 작동
- **프로세스 노트**: 브라우저 자동화 중 스크롤 직후 읽은 버튼 ref로 클릭했다가 페이지가 예상과 다른 단계(4→3단계로 역행)로 이동하는 현상을 겪음 — DOM에서 텍스트로 버튼을 찾아 클릭하는 방식(`document.querySelectorAll('button')`에서 텍스트 매칭)으로 전환해 이후 안정적으로 재현 가능한 네비게이션을 확보함(참고용 기록, 코드 변경 아님)

**이 시점 기준(2026-08-12) 경기도 전역 확대 작업이 데이터·코드·UI·브라우저 검증까지 전부 완료됨.** 항목 23에서 정의한 "데이터는 경기도 전체, 이동시간은 선택 지역만 온디맨드" 전략이 실제로 작동함을 신규 지역(과천시)과 기존 검증 지역(연천군) 양쪽에서 확인. 요구사항정의서 2.3절("1차 개발·검증 단계는 경기도 연천교육지원청을 시범 지역으로 확정")이 이제 실제 구현 범위(경기도 전역 선택 가능)보다 좁게 남아 있어, 이 절을 갱신할지는 사용자 확인이 필요함(다음 세션 후보 작업).

## 2026-08-12 (계속) — 사용자 검토 중 발견된 이슈 3건

### 25. 6단계 지도 중심점 버그 수정 + 2단계 학교선택 범위 논의(보류)

- 사용자가 스트림릿 앱을 직접 열어 검토하던 중 "6단계 지도가 왜 경기도 전체로 보이냐"고 질문
- **원인 확인**: `app/streamlit_app.py` 6단계(`render_step6`)가 `da.load_school_dim()`(지역 필터 없는 경기도 전체 2,050개교)로 지도 중심 좌표(위경도 평균)를 계산하고 있었음 — 항목 24에서 5단계 CPMP 후보군 버그는 고쳤지만 6단계 지도 코드는 별도 위치라 놓쳤던 것. 실측: 수정 전 중심 (37.4348, 127.0282, 안양·과천·성남 부근) → 수정 후 중심 (38.0624, 127.0470, 연천군 좌표 범위 37.998~38.183/126.908~127.137 내부). `da.schools_in_region(ss.selected_region)`으로 교체해 수정
- **부가 질문 대응**: "존치(거점학교)"인데 통학시간이 None인 학교(연천노곡초등학교)에 대해 항목 20에서 이미 규명한 원인(Σ Y_j=p 등식 제약으로 존치이지만 배정 학생 0명이 될 수 있음, 버그 아님)을 재설명
- **2단계 "다음 단계" 버튼 미작동 신고**: 동일 시나리오(연천군, 전체 학교 Select all)를 브라우저로 직접 재현한 결과 코드는 정상 작동(버튼 disabled 조건은 미선택 시뿐, 선택 후 3단계까지 정상 진입 확인) — 실제 원인은 "Select all" 클릭 후 열린 채 남는 드롭다운 목록 패널이 버튼을 가리거나 클릭을 가로챈 것으로 추정(재확인 필요, 코드 수정 안 함)
- **2단계 선택-시뮬레이션 무관 문제 발견**: 코드 검토 중 "분석 대상 학교"(2단계 체크박스 선택)가 3단계 통계 카드 표시에만 쓰이고, 5~6단계 CPMP 후보군(`candidates = dim[dim["school_level"]==ss.grid_level]`)은 2단계 선택과 무관하게 항상 지역 전체 학교를 사용함을 발견 — 요구사항정의서 5.5절과 모순은 아니나(문서에 "2단계 선택만 후보"라는 명시 없음), UI상 2단계가 필수 선택처럼 보이는 것과 실제 동작이 달라 혼란 소지. 사용자에게 (A)선택 학교만 후보로 반영 (B)UI 캡션만 명확화 (C)보류 세 가지 중 선택 요청 → **"일단 보류, 다른 작업 먼저"로 확인** — 코드 변경 없이 이 기록만 남기고 다음 세션 후보로 유지

### 26. 2단계를 "학교 선택"에서 "지역 학교 현황"으로 변경 + 6단계 "존치(배정 없음)" 표현 개선

- 항목 25에서 보류했던 "2단계 선택이 시뮬레이션과 무관" 문제에 대해, 사용자가 방향을 다시 지시: "학교선정하도록 하지 말고, 지역 내 초등(중)학교 현황 보여주는 페이지를 거치고 시뮬레이션으로 가도록 한다" — 즉 (A)선택을 시뮬레이션에 반영 대신, 애초에 "선택" 개념 자체를 없애고 2단계를 순수 현황 확인 화면으로 바꾸는 방향으로 결정
- [app/streamlit_app.py](app/streamlit_app.py) `render_step2()` 수정: 학교급·설립구분 필터로 걸러진 학교 목록에서 사용자가 개별로 체크하던 `st.multiselect("분석 대상 학교 선택 ...")`를 제거. 필터링된 학교 전체를 자동으로 `ss.selected_school_codes`에 채우고, "다음 단계" 버튼은 선택 여부가 아니라 "필터 결과가 비어있는지"로만 비활성화 조건을 바꿈(실질적으로 거의 항상 활성화). 화면 상단에 "5~6단계 시뮬레이션은 4단계에서 고르는 학교급 기준으로 이 지역의 모든 학교를 후보로 사용합니다"라는 캡션을 추가해, 이 화면이 순수 정보 확인용이고 시뮬레이션 후보는 항상 지역 전체임을 명시적으로 알림(항목 25에서 발견한 오해 소지를 코드·문구 양쪽에서 해소)
- `STEPS` 목록의 2단계 이름을 "학교 옵션 선택" → "지역 학교 현황"으로 변경(사이드바 진행 단계 표시에도 반영)
- **6단계 "존치인데 배정 학생 0명"에 대한 표현 개선**: 사용자가 "일반인에게는 '존치인데 학생이 없다'는 뜻으로 오독될 수 있다"고 지적 — 항목 20에서 이미 정상 현상으로 규명했던 것을 이번엔 UI 표현으로 직접 해소. 결과표의 "구분" 컬럼을 기존 2분류(존치(거점학교)/폐교대상)에서, 존치이지만 배정 학생이 0명인 학교만 별도로 "존치(배정 학생 없음)"으로 3분류. 평균/최대 통학시간 셀의 `None` 표시도 읽기 어려운 파이썬 표기라 "-"로 교체. 표 아래에 해당 학교가 있을 때만 뜨는 안내문 추가: "이 학교는 폐교 대상이 아니라 계속 운영되는 학교입니다. 다만 이번 시뮬레이션에서는... 배정된 학생이 없다는 뜻입니다(모형이 정확히 p개교를 존치하도록 강제하는 제약 때문... 실제로 학생이 0명이라는 의미는 아닙니다)"
- 브라우저로 연천군 전체 재검증(로그인→1~6단계): 2단계에 필터만 남고 선택 UI 없이 "다음 단계" 바로 활성화됨을 확인, 3단계가 19개교(초13+중6) 전량 자동 표시됨을 확인(기존과 동일한 학생수·백분위 값 재현 — 회귀 없음), 6단계 목적함수 4,624 그대로 재현(회귀 없음), 새 안내문이 "'존치(배정 학생 없음)' 1개교는..."으로 정확히 1개교(연천노곡초등학교)를 가리키며 표시됨을 확인
- **문서 반영 남음(TODO)**: 요구사항정의서 5.2절("사용자가 분석 대상 학교를 선택하면...")이 이번 변경으로 실제 동작과 달라졌음 — 다음 문서 정리 시점에 python-docx로 갱신 필요

### 27. 2단계 학교급·설립구분을 다중선택→라디오버튼(단일선택)으로 변경 + 6단계에 "통합 전후 비교" 신규 추가

- 사용자가 항목 26 검토 중 지적: "초등학교와 중학교를 모두 탭에 넣어서 분석하는데, 결과는 초등학교만 나온다. 이거는 아예 옵션을 라디오 버튼으로 하고 둘 중 하나만 선택하도록 하면 어떤가? 공립/사립도 마찬가지" — 2단계의 `st.multiselect("학교급", ...)`/`st.multiselect("설립구분", ...)`가 여러 개를 동시에 고를 수 있는 것처럼 보이지만, 실제 CPMP 후보군은 4단계에서 고르는 단일 학교급만 쓴다는 점(항목 24~26에서 이미 확인한 구조)과 UI가 어긋나 있다는 지적
- [app/streamlit_app.py](app/streamlit_app.py) `render_step2()` 수정: 두 필터를 `st.radio(..., horizontal=True)`로 교체. 특히 "학교급" 라디오는 아예 `ss.grid_level`(4단계 CPMP 후보 선정에 쓰이는 바로 그 변수)에 직접 바인딩해, 2단계에서 고른 학교급이 4단계·5단계까지 하나의 값으로 그대로 이어지도록 함(중복된 별도 상태를 만들지 않음). 브라우저 재검증: 2단계에서 "초등학교" 선택 시 3단계에 13개교(기존 19개교에서 중학교 6개 제외)만 표시됨을 확인 — 필터가 다운스트림까지 정확히 반영됨
- **6단계 "통합 전후 비교" 신규 추가**: 사용자가 "3개 학교가 통폐합됐다면 좋아진 것은 무엇인가? 통합 전후 비교가 시뮬레이션 결과에 있어야 하지 않나"라고 지적 — 기존엔 통합 후(목적함수값) 숫자만 있고 비교 기준선이 전혀 없었음
  - [backend/algo/cpmp.py](backend/algo/cpmp.py)에 `baseline_nearest_assignment()` 신규 함수 추가: 대상 학교급의 현존 학교를 전부 존치한 채(수용상한·이동시간제한 없이) 각 수요지를 순수 최근접 학교로 배정하는 "통합 전(현재 상태)" 기준선을 계산(LP를 풀지 않고 단순 argmin이라 항상 즉시 계산됨, `solve_cpmp`와 동일한 `CpmpResult` 구조로 반환해 6단계에서 그대로 나란히 씀)
  - [app/streamlit_app.py](app/streamlit_app.py) 5단계에서 "시뮬레이션 시작" 시 `result`(통합 후)와 `baseline`(통합 전)을 함께 계산해 `ss.sim_baseline`에 저장
  - 6단계에 "통합 전후 비교" 섹션 추가: 학교 수·학교당 평균 학생수·총 가중 통학시간·평균 통학시간(1인당)을 통합 전/후 나란히 표시. 통학시간이 통합 후 늘어날 수 있다는 것과, 통폐합의 목적이 통학시간 단축이 아니라 학교 규모 적정화라는 것을 캡션으로 명시(정책적 오해 방지)
  - **브라우저로 두 시나리오 검증**: (1) 기본값(p=10, 13개교 중 3개교 폐교) — 통합 전후 총 가중 통학시간이 4,624로 완전히 동일(+0)하게 나옴. 처음엔 버그로 의심했으나, 폐교 대상 3개교가 마침 "누구의 최근접 학교도 아니었던" 학교들이라 폐교해도 아무도 재배정될 필요가 없어 최적해가 기준선과 정확히 일치하는 것이 수학적으로 타당함을 확인(제약4의 수용상한이 넉넉해 최적화가 굳이 최근접 배정을 흐트러뜨릴 이유가 없었음). (2) 검증을 위해 p=1(13개교→1개교로 극단적 통합)로 재실행 — 통합 전 4,624 → 통합 후 8,397(+3,773, 평균 통학시간 1인당 +3.1분)로 뚜렷하게 증가함을 확인, 기능이 시나리오에 따라 정확히 다른 값을 내는 것을 검증(회귀 없음, 버그 아님)
  - **프로세스 노트**: `backend/algo/cpmp.py`에 함수를 추가한 뒤 streamlit_app.py의 import 라인만 rerun에는 반영되고 cpmp.py 자체는 `sys.modules` 캐시 때문에 반영 안 돼 `ImportError` 발생(항목 8에서 이미 알려진 패턴 재발) — 스트림릿 프로세스를 완전히 재시작(preview_stop → preview_start)해서 해결

### 28. 6단계 지도 개선(원 크기=규모, 굵은 시군구 경계) + 5단계에 "학교 수-통학시간" 박스플롯 신규 추가

- 사용자가 KEDI 학교입지재편 연구(김지우)의 캡처 이미지 2장을 제시하며 유사한 시각화를 요청: (1) 6단계 결과 지도에서 원 크기로 학교 규모(학생수)를, 색상으로 배정 학교를, 굵은 선으로 기초자치단체 경계를 표시 (2) 5단계 진입 전(시뮬레이션 실행 전) 통폐합 대상 학교 비율(%) 대비 통학시간(분) 분포를 박스플롯으로 미리 보여주는 그래프. 두 이미지 자체는 저작물이라 그대로 복제하지 않고, 우리 앱의 실제 데이터로 같은 방식의 시각화를 새로 구현하는 방향으로 진행
- **6단계 지도**: [app/streamlit_app.py](app/streamlit_app.py) `render_step6()` 수정
  - `da.load_admin_boundary(ss.selected_region)`를 지도에 추가(굵은 검은선, weight=4, 채우기 없음) — 기존엔 1단계에만 있던 시군구 경계를 6단계 지도에도 표시
  - 존치 학교 마커 반지름을 배정 학생수(`school_load`)에 비례하도록 변경(4~24px, 최대 배정교 기준 선형 스케일). 폐교대상·배정 0명 학교는 최소 반지름(4px)으로 축소
  - HTML 범례를 "학생 수(원 크기)"(4단계 대표값, 실제 배정 학생수 분포에서 동적으로 산출)와 "배정 학교(색상=학구)"(학교명별 색상 원형 스와치) 두 블록으로 분리해 나란히 배치
  - 브라우저 검증: 연천군 p=10 기본 시나리오에서 두 범례가 각각 "180/360/530/710명"(실제 데이터 기준 자동 산출)과 10개 존치 학교명+폐교대상으로 정상 렌더링, 목적함수 4,624 회귀 없음 확인. 실제 지도 위 원 크기·굵은 경계선은 folium이 크로스오리진 iframe으로 렌더링되어 화면 캡처로 직접 확인은 못 했으나(항목 25와 동일한 도구 한계), 코드 로직은 1단계에서 이미 검증된 경계 렌더링 패턴을 그대로 재사용한 것이라 신뢰도 높음
- **5단계 "학교 수(통폐합 비율)에 따른 통학시간 분포" 박스플롯 신규**: [backend/algo/cpmp.py](backend/algo/cpmp.py)에 `p_sweep_travel_minutes()` 추가 — 대상 학교급의 잔존 학교 수(p)를 1~전체 구간(학교 많은 지역은 최대 30개 표본으로 제한)으로 훑으며, 수용상한·이동시간제한을 사실상 무제한으로 두고(순수 지리적 최적 배정) CPMP를 반복 실행해 통폐합 비율(%)별 통학시간 분포를 계산
  - [app/streamlit_app.py](app/streamlit_app.py) 5단계에서 파라미터 슬라이더 아래·"시뮬레이션 시작" 버튼 위에 altair 박스플롯으로 표시(altair는 스트림릿 기본 차트가 이미 의존하는 라이브러리라 신규 패키지 설치 없이 사용 가능함을 확인 — plotly/matplotlib은 미설치 상태였음). 세션 상태에 지역·학교급·이동시간소스 조합별로 캐싱해(`p_sweep_{region}_{level}_{kakao|approx}`) 슬라이더를 움직일 때마다 재계산하지 않도록 함. 이 그래프는 사용자가 설정한 수용상한·이동시간제한과 무관한 별개의 탐색 결과라는 점을 캡션으로 명시(아래 실제 시뮬레이션과 값이 다를 수 있음을 미리 안내)
  - 브라우저 검증: 연천군 초등학교 기준 x축이 0~92%(13개교 기준 1~13개교 단계) 통폐합 비율, y축 0~25분 이동시간으로 정상 렌더링 확인
- **문서 반영 남음(TODO)**: 요구사항정의서 5.5·5.6절에 이번에 추가된 두 시각화(사전 탐색 박스플롯, 개선된 결과 지도)가 아직 반영 안 됨 — 다음 문서 정리 시점 TODO로 남김

### 29. 굵은 행정구역 경계를 2·4단계 지도에도 확장 + 박스플롯 해석 각주·가독성 개선

- 사용자가 2단계 지도를 보고 "왜 행정구역 경계가 굵은 선으로 표시되지 않냐"고 지적(항목 28에서 6단계에만 반영했던 것), 이어서 "4단계에서도" 요청
- [app/streamlit_app.py](app/streamlit_app.py): 굵은 경계선 렌더링 코드가 1·2·4·6단계 네 곳에 쓰이게 되어 `add_region_boundary(m, region)` 공통 함수로 추출(1단계는 데이터 소스 상태 표시라는 별도 목적이 있어 기존 얇은 파란선 스타일 그대로 유지, 나머지 세 곳만 공통 함수로 교체). 브라우저로 2·3·4·5단계까지 오류 없이 정상 진행되는 것을 확인(회귀 없음)
- **박스플롯 해석 질문 대응**: 사용자가 "오른쪽으로 갈수록 학교 수가 남아있는 경우 통학시간을 보여주는 것이냐"고 질문(정확한 이해였음) — 가로축 제목에 "오른쪽일수록 잔존 학교 수(p) 적음"을 직접 병기하고, 그래프 아래에 "※ 그래프 해석" 각주를 추가해 가로축·세로축·상자그림 구성요소(사분위·중앙값·수염·이상치)를 상세히 설명. 축 폰트 크기도 키움(labelFontSize 13, titleFontSize 14) — 기존엔 작아서 안 보인다는 지적 반영
- 브라우저 검증: 연천군 초등학교 기준 각주 텍스트와 확대된 축 글자가 정상 렌더링됨을 확인

### 30. 5단계 "시뮬레이션 시작" 시 진행률 게이지바 추가

- 사용자가 "시뮬레이션 진행중이라고 뜨면서 게이지바가 올라가는 것을 보여주고 싶다"고 요청 — 기존엔 `st.spinner("CPMP 연산 중...")`로 회전 아이콘만 표시되고 진행률은 보이지 않았음
- [app/streamlit_app.py](app/streamlit_app.py): `st.progress()`로 교체, 파이프라인의 실제 단계(수요지·학교 후보 구성 → CPMP 최적화 연산 → 통합 전 기준선 계산 → 완료)에 맞춰 0→35→85→100%로 올라가도록 구현. CPMP 솔버 자체는 PuLP/CBC라 내부 반복 진행률을 직접 얻을 수 없어, 각 단계 구간 안에서는 `time.sleep(0.05)` 간격으로 5%씩 증가시켜 게이지가 매끄럽게 올라가는 것처럼 보이게 함(완전한 가짜 애니메이션은 아니고, 실제 파이프라인 단계 경계는 정확히 반영됨)
- 브라우저 검증: 목적함수 4,624 그대로 재현(회귀 없음). 다만 연천군 규모 연산이 워낙 빨라(전체 게이지 애니메이션 약 1초) 브라우저 자동화 도구의 네트워크 왕복 지연으로는 중간 진행 상태를 캡처하지 못함 — 실제 사용자 화면에서는 정상적으로 게이지가 차오르는 것이 보일 것으로 예상(코드 검토로 확인, 실사용 확인은 사용자에게 요청)

### 31. 3단계 4개 지표에 경기도 평균 병기

- 사용자 요청: 학급당 학생수·교사1인당 학생수·학생1인당 기본적/선택적교육활동비 옆에 "해당 광역단체(경기도) 평균값"을 병기
- [app/data_access.py](app/data_access.py): `province_averages(school_level)`(경기도 전체 동일 학교급 가중평균 = 총학생수÷총학급수·총학생수÷총교사수, 학교별 최신연도 기준), `province_budget_averages(school_level)`(예산자료 확보 학교들의 가중평균) 두 함수 추가, 둘 다 `@lru_cache`로 세션 내 재계산 방지
- **데이터 정직성 이슈 발견 및 처리**: 학급당 학생수·교사1인당 학생수는 school_year_stat이 경기도 전역을 커버해 진짜 "경기도 평균"을 계산할 수 있지만, 교육활동비 예산자료(`school_budget_2025.csv`)는 여전히 연천군 19개교만 확보된 상태(항목 22)라 "경기도 평균"이라고 표시하면 사실과 다름 — 캡션에 "평균(예산자료 확보 N개교 기준, 경기도 전역 아님)"이라고 정확한 범위를 명시. 다른 지역 예산자료가 추가되면 함수 코드 변경 없이 범위가 자동으로 넓어짐
- [app/streamlit_app.py](app/streamlit_app.py) 3단계에 각 지표 `st.metric` 바로 아래 `st.caption`으로 병기
- 브라우저 검증: 연천군 초등학교 전체(13개교)에서 "경기도 초등학교 평균 20.2명"(학급당)·"16.5명"(교사1인당)이 모든 학교에 동일하게(지역 아닌 전체 평균이므로 타당) 표시되고, 예산 캡션은 "평균 799,808원(예산자료 확보 13개교 기준, 경기도 전역 아님)"으로 정확한 범위 한정 문구와 함께 표시됨을 확인

<!-- 새 결정은 아래에 "## YYYY-MM-DD" 형식으로 추가 -->
